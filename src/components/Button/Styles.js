import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    height: 59,
  },
  btnDefault: {
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    width: '100%',
    height: '100%',
  },
  btnTitle: {
    color: 'white',
    letterSpacing: 1.25,
    marginLeft: 'auto',
    marginRight: 'auto',
  },
});
